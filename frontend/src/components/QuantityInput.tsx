import React from 'react'; export default function QuantityInput({value,onChange}:{value:number;onChange:(v:number)=>void}){return(<div style={{display:'inline-flex',border:'1px solid #ddd',borderRadius:8}}><button onClick={()=>onChange(Math.max(1,value-1))} style={{padding:'6px 10px'}}>-</button><input value={value} onChange={e=>onChange(Math.max(1,parseInt(e.target.value||'1')))} style={{width:40,textAlign:'center',border:'none'}}/><button onClick={()=>onChange(value+1)} style={{padding:'6px 10px'}}>+</button></div>);}
