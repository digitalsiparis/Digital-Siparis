{
  "name": "@digital-siparis/db",
  "version": "0.1.0",
  "type": "module",
  "prisma": { "schema": "prisma/schema.prisma" },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init",
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1"
  },
  "devDependencies": {
    "prisma": "^5.19.1",
    "tsx": "^4.19.1"
  }
}
